# Security

This portfolio repository does not contain production credentials, customer data, or commercial image assets.

- Keep `.env` and `.dev.vars` untracked.
- Store production secrets with `wrangler secret put`.
- Replace the example Cloudflare resource identifiers before deployment.
- Protect the operations UI with Cloudflare Access.
- Report suspected credential exposure privately to the repository owner rather than opening a public issue.
