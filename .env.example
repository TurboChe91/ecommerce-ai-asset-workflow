# Local identity used by the development server only.
DEV_AUTH_EMAIL=local-operator@example.com

# Non-secret deployment configuration. Copy these values into wrangler.jsonc.
TEAM_DOMAIN=https://your-team.cloudflareaccess.com
POLICY_AUD=replace-with-your-access-audience
MAX_UPLOAD_BYTES=12582912
